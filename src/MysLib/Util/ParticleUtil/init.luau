local ParticleUtil = {}

ParticleUtil.emitParticle = require(script.emitParticle);
ParticleUtil.playParticleEffects = require(script.playParticleEffects);
ParticleUtil.setParticlesEnabled = require(script.setParticlesEnabled);
ParticleUtil.setBeamsEnabled = require(script.setBeamsEnabled);
ParticleUtil.clearAllParticles = require(script.clearAllParticles);

return ParticleUtil
